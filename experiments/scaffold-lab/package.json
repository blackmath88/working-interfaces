{
  "name": "@working-interfaces/scaffold-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "tokens": "node ../../apps/field-manual/scripts/sync-downloads.mjs",
    "dev": "npm run tokens && vite",
    "build": "npm run tokens && tsc --project tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@working-interfaces/scaffold": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.1"
  }
}
