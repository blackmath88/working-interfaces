# Decision records

Decision records use a four-digit, repository-wide sequence in their filename:
`NNNN-short-title.md`. Each new decision takes the next unused number; numbers
are never reused, even when a decision is superseded.
